# Components page restructure — deferred remainder
Status: open
Priority: later

## Deferred question

Whether the **Front-end integrations** section (the `joe mcp` MCP server and the
`joe slack` Slack bot) belongs on the Components section index at all. MCP and Slack
are *surfaces of Joe* — front-ends that connect *to* a running daemon — rather than
components Joe manages, so their presence on a page about registering managed systems
is arguably a category mismatch. Candidate new homes are **Guides** or a dedicated
section of their own. Not acted on in the `components-page-restructure` session, which
scoped itself to extracting the per-type routing tables and the not-yet-supported list
into the `connectable-systems` child page.
