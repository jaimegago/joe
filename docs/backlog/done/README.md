# Completed backlog items

Finished backlog items move here from `docs/backlog/`. Files in this directory
are archived: they are retained for history but are excluded from
[`../INDEX.md`](../INDEX.md), which only summarizes the active items directly
under `docs/backlog/`.

This README exists so the directory is tracked by git while empty. It can stay
once real archived items land alongside it.
