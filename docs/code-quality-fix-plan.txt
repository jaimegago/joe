Code Quality Fix Plan
=====================
Reference: docs/code-quality-report.txt
Order: highest severity and most impactful first


STEP 1 — B1: Fix AddMessages to enforce MaxMessages [DONE]
----------------------------------------------------
File: internal/useragent/session.go

What was done:
  - Extracted pruning into private pruneMessages() method
  - Both AddMessage and AddMessages now call pruneMessages()
  - Added TestSession_AddMessages_Pruning test


STEP 2 — A3: Improve pruning strategy (related to B1) [DONE]
------------------------------------------------------
File: internal/useragent/session.go

What was done:
  - Changed pruning from "keep MaxMessages/2" to sliding window:
    "keep last MaxMessages messages"
  - Updated all pruning tests to reflect new behavior


STEP 3 — B4: Symlink-resolve cleanJoeDir in IsPathAllowed [DONE]
----------------------------------------------------------
File: internal/safety/invariants.go

What was done:
  - Added resolvePathSymlinks(cleanJoeDir) after computing cleanJoeDir
  - Also resolved symlinks for safetyPolicyPath
  - Moved symlink resolution before case normalization for correctness
  - Existing symlink test already covered this scenario


STEP 4 — B2: Replace Unix-timestamp source IDs with UUIDs [DONE]
----------------------------------------------------------
File: internal/coreagent/agent.go

What was done:
  - Used crypto/rand to generate 8 random bytes as hex suffix
  - Changed from fmt.Sprintf("%s-%d", sourceType, time.Now().Unix())
    to fmt.Sprintf("%s-%x", sourceType, randBytes)
  - Removed unused "time" import


STEP 5 — B3: Replace error string comparison with sentinel error [DONE]
----------------------------------------------------------------
Files: internal/store/sources.go, internal/coreagent/agent.go,
       internal/api/server.go, internal/api/helpers.go

What was done:
  - Defined store.ErrSourceNotFound sentinel in store/sources.go
  - coreagent wraps it: fmt.Errorf("%w: %s", store.ErrSourceNotFound, ...)
  - api/server.go uses errors.Is(err, store.ErrSourceNotFound)
  - api/helpers.go replaced local errSourceNotFound with store.ErrSourceNotFound
  - Updated test in control_test.go


STEP 6 — B5: Use errors.Is instead of == for sql.ErrNoRows [DONE]
-----------------------------------------------------------
File: internal/graph/sqlite.go

What was done:
  - Changed all 3 sites (GetNode, Path, getEdgeBetween) from
    err == sql.ErrNoRows to errors.Is(err, sql.ErrNoRows)
  - Added "errors" to imports


STEP 7 — B6: Sort allowed-command lists for deterministic output [DONE]
----------------------------------------------------------------
Files: internal/tools/local/runcmd/runcmd.go,
       internal/tools/local/runcmd/subcommands.go

What was done:
  - Added sortedAllowedCommands() helper method to Tool
  - Description() and Execute() both use it (fixes DRY too)
  - Added sort.Strings(keys) in allowedKeys() in subcommands.go


STEP 8 — D1: Eliminate throwaway Server delegation in API handlers [DONE]
------------------------------------------------------------------
File: internal/api/server.go

What was done:
  - Gave each handler struct (sourceHandler, k8sHandler, gitHandler,
    awsHandler) a *Server reference instead of store/registry/metrics
  - Simplified register*Routes to take only (mux, prefix)
  - All handler methods now delegate via h.server.handleXxx(w, r)
  - Removed all 14 ephemeral &Server{...} constructions
  - Updated routes_test.go


STEP 9 — D2: Consolidate tilde expansion into a shared package [DONE]
--------------------------------------------------------------
Files: internal/paths/defaults.go, internal/tools/local/pathutil.go,
       internal/config/config.go

What was done:
  - Added paths.ExpandPath() using getSecureHomeDir()
  - internal/tools/local/pathutil.go now delegates to paths.ExpandPath()
  - config.go Load and Save use paths.ExpandPath() instead of inline code


STEP 10 — G1: Remove claude import from config package [DONE]
-------------------------------------------------------
File: internal/config/config.go, internal/config/constants.go

What was done:
  - Added defaultLLMModel = "claude-sonnet-4-20250514" to constants.go
  - Replaced claude.DefaultModel with defaultLLMModel
  - Replaced claude import with paths import


STEP 11 — G2: Use errors.As in IsInvariantViolation [DONE]
----------------------------------------------------
File: internal/safety/invariants.go

What was done:
  - Changed from direct type assertion to errors.As(err, &e)
  - Added "errors" to imports


STEP 12 — G3: Pass context.Context to session methods [DONE]
------------------------------------------------------
File: internal/useragent/session.go, internal/useragent/agent.go

What was done:
  - AddMessage, AddMessages, AddTokenUsage all accept ctx context.Context
  - Replaced context.Background() with ctx in metrics calls
  - Updated all callers in agent.go to pass ctx
  - Updated all tests to pass context.Background()


STEP 13 — G4: Standardize on secure home dir resolution [DONE]
--------------------------------------------------------
Files: internal/adapters/git/git.go

What was done:
  - git.go repoDir() now uses paths.JoeDirPath() instead of os.UserHomeDir()
  - Removed unused "os" import from git.go
  - Verified remaining os.UserHomeDir() calls are only in tests and k8s
    kubeconfig (standard practice)


STEP 14 — G5: Replace isCaseInsensitiveFS() calls with package-level var [DONE]
-------------------------------------------------------------------------
File: internal/safety/invariants.go

What was done:
  - Added package-level var caseInsensitiveFS = runtime.GOOS == "darwin" || ...
  - Replaced all 5 isCaseInsensitiveFS() calls with caseInsensitiveFS
  - Removed the isCaseInsensitiveFS() function
  - Updated invariants_test.go reference


STEP 15 — D4: Log metadata JSON unmarshal errors instead of silently discarding [DONE]
--------------------------------------------------------------------------------
File: internal/graph/sqlite.go

What was done:
  - Added slog.Warn for both GetNode and scanNodes metadata unmarshal errors
  - Added "log/slog" to imports
  - No edge metadata unmarshal exists (edges don't have metadata)


STEP 16 — A1: Document AddNode upsert behavior [DONE]
-----------------------------------------------------------------
Files: internal/graph/store.go, internal/graph/sqlite.go,
       internal/coreagent/agent.go

What was done:
  - Added doc comment to GraphStore.AddNode interface explaining upsert
  - Added doc comment to SQLiteStore.AddNode implementation
  - Replaced TODO comment in coreagent/agent.go with accurate explanation


STEP 17 — A2: Fix Path() cycle detection substring false positives [DONE]
------------------------------------------------------------------
File: internal/graph/sqlite.go

What was done:
  - Changed initial path to ',' || ? || ',' (delimiter-wrapped)
  - Changed path append to: p.path || next_node || ','
  - Changed cycle check from '%' || node || '%' to '%,' || node || ',%'
  - Added strings.Trim when parsing pathStr back to node list
  - Added TestPath_SubstringNodeIDs test verifying "pod" vs "pod-abc"


STEP 18 — Final verification [DONE]
-----------------------------
Results:
  a) go build ./...          PASS (compiles cleanly)
  b) go vet ./...            PASS (no warnings)
  c) gofmt -s -w .           PASS (all formatted)
  d) go test ./...           PASS (all internal tests pass; pre-existing
     integration test failure in test/integration for AWS type assertions
     is unrelated to these changes)
  e) Coverage maintained at >80%
