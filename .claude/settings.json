{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | xargs -I {} bash -c 'if [[ \"{}\" == *.go ]]; then gofmt -s -w \"{}\"; fi'"
          }
        ]
      }
    ]
  }
}
